//import { Timestamp } from '@react-native-firebase/firestore';
export type ProfileDTO = {
    // photoUrl: string,
    // emailVerified: boolean,
    userId: string,
    name: string,
    email: string,
    objective: string,
    weight: string,
    tall: string,
    sex: string,
    //birthDate: Timestamp,
    userType: string
}